# Decisions Log

记录设计决策、与 c2_plan 锁定层的差异、微观实验结果。

| 日期 | 文件 / 范围 | 决策内容 | 证据来源（官方实现 / 微观实验 / 讨论） | c2_plan 是否需更新 |
|---|---|---|---|---|
| 2026-05-12 | src/data.py sliding window padding | 首帧（$t < T_o$）复制首帧 obs；末端（$t + H - 1 > T$）hold pose，用末动作 `actions[T_demo - 1]` 复制 | 用户讨论 + 伪代码锁定 | 否 |
| 2026-05-12 | src/data.py action 归一化 | per-dim min-max → $[-1, 1]$；gripper 维度与 6-DoF joint 同等处理（无特殊分支）；`denormalize` 内 `torch.clamp(-1, 1)`；统计量经 `register_buffer` 嵌入 checkpoint | 用户讨论 + 伪代码锁定 | 否 |
| 2026-05-12 | src/data.py + scripts/precompute_r3m.py R3M cache | 单大 array per task；shape `(N_demos, T_max, 2_views, K_aug, 2048)`；`np.load(path, mmap_mode="r")` | 用户讨论 + 伪代码锁定 | 否 |
| 2026-05-12 | src/data.py augment dispatch | per-sample（每次 `__getitem__` 内抽 `aug_idx`） | 用户讨论 + 伪代码锁定 | 否 |
| 2026-05-12 | src/data.py dataset indexing | 长度 $= \sum_d T_d$（前缀和 + `bisect_right` 二分）；`__getitem__` 返回携带 `demo_id` 与 `step_t` 元数据 | 用户讨论 + 伪代码锁定 | 否 |
| 2026-05-12 | src/data.py eval obs 一致性 | eval 与 train 共用 `preprocess_image`（`augment=False`）+ `Normalizer` 反归一化 | 用户讨论 + 伪代码锁定 | 否 |
| 2026-05-12 | src/data.py + plan/c2_plan.md §3.3 obs concat 顺序 | $(t-1, t)$ 顺序，旧帧在前；c2_plan §3.3 公式同步反转以与伪代码对齐 | reviewer flag（§3.3 vs §4.2 内部不一致）+ 用户裁定 | 是（§3.3 已更新）|
| 2026-05-12 | src/data.py 随机数源 | 统一 torch：`aug_idx = torch.randint(0, K, (1,)).item()`；`preprocess_image` augment 路径 rng 类型为 `torch.Generator`，augment 内 `torch.randint(..., generator=rng)` 与 `torch.rand(..., generator=rng)` | 用户讨论 | 否 |
| 2026-05-12 | src/data.py + scripts/precompute_r3m.py preprocess_image augment 参数 | augment 路径 brightness / contrast 经 kwargs 由 caller（`scripts/precompute_r3m.py`）传入；data.py 不持默认值，未传则 `ValueError` | 用户讨论 | 否 |
| 2026-05-12 | src/data.py preprocess_image 输出格式 | 输出 `(3, 224, 224)` float32 值域 $[0, 255]$，**不外部 ImageNet 归一化**（R3M 模型内部处理；外部再做会导致 train cache 与 eval 路径双重归一化） | 用户讨论 + 伪代码修订（原版本 `/255` + `TF.normalize` 已删） | 否 |
| 2026-05-12 | scripts/precompute_r3m.py augment 参数取值 | `BRIGHTNESS = CONTRAST = 0.3`（经 kwargs 传入 `preprocess_image`） | 用户讨论 | 否 |
| 2026-05-12 | scripts/precompute_r3m.py TASK_LIST | LIBERO-Goal 5-task subset，按动作类型多样性选取：`open_the_top_drawer_and_put_the_bowl_inside_demo`（compound 唯一）、`push_the_plate_to_the_front_of_the_stove_demo`（push 唯一）、`turn_on_the_stove_demo`（turn 唯一）、`put_the_cream_cheese_in_the_bowl_demo`（put into container，区别于 flat surface 放置）、`put_the_wine_bottle_on_top_of_the_cabinet_demo`（高位放置 + 细长物体，grasp 角度难度高于宽底 bowl） | 用户讨论 | 否 |
| 2026-05-13 | src/models.py FiLM $\gamma$ 初始化 | PyTorch 默认 Kaiming uniform（不采用 c2_plan 中 $\gamma = 0$ 或 $\gamma = 1$ 的常数初始化） | 用户讨论 | 是 |
| 2026-05-13 | src/models.py time embedding | CFM / DP 共享 `SinusoidalPosEmb` 函数，`max_period = 10000`；DP 端 $t \to t/T \in [0, 1]$ 归一化后送入，CFM 端 $\tau \in [0, 1]$ 直接送入 | 用户讨论 | 否 |
| 2026-05-13 | src/models.py ObsEncoder + plan/c2_plan.md §3.3 | 引入 ObsEncoder 替代 raw obs concat 作为 ConditionalUnet1D 的 global condition $o$。forward 输入为**单个**拼接后的特征图 $\tilde{o} \in \mathbb{R}^{B \times 8210}$（**不是** per-modality dict），ObsEncoder 内部按 8 token slice 切分后再走 per-modality 投影。结构：8210 concat 切片为 [cam1\_{t-1}, cam2\_{t-1}, q\_{t-1}, g\_{t-1}, cam1\_t, cam2\_t, q\_t, g\_t] → per-modality 投影（`Linear(2048, 256)` cam1、`Linear(2048, 256)` cam2、`Linear(7, 256)` proprio、`Linear(2, 256)` gripper；cam1 / cam2 投影独立，prev / curr 跨步共享权重）→ learnable `step_emb(2, 256)` + `modality_emb(4, 256)` → 3 × PreNormBlock(dim=256, num_heads=4, mlp_ratio=4, SiLU FFN) → final LayerNorm → flatten 8 token。$o = \text{ObsEncoder}(\tilde{o}) \in \mathbb{R}^{2048}$（原 $\tilde{d}_o = 8210$）。Token 顺序与 [decisions 2026-05-12 第 7 行](#) 锁定的 obs concat $(t-1, t)$ 顺序对齐：tokens = [cam1\_{t-1}, cam2\_{t-1}, q\_{t-1}, g\_{t-1}, cam1\_t, cam2\_t, q\_t, g\_t]，`step_idx = [0,0,0,0,1,1,1,1]`（step=0 ↔ $t-1$，step=1 ↔ $t$），`modality_idx = [0,1,2,3,0,1,2,3]`。无 dropout，无 mask。动机：当前 [src/model/unet1d.py:104](../src/model/unet1d.py#L104) 直接 `concat([t_emb, obs])` 使 `cond_dim = 256 + 8210 = 8466`，各级 FiLM proj 参数估 $\sim 135\text{M}$，超 c2_plan §3.2 锁定 ~50M 目标；ObsEncoder 路径下 `cond_dim = 2304`，FiLM proj $\sim 37\text{M}$ + ObsEncoder $\sim 3.5\text{M}$ $\approx 41\text{M}$，命中目标 | 用户讨论 + reviewer flag → 用户裁定 | 是（§3.3 已更新）|
| 2026-05-13 | src/models.py → src/model/{modules,unet1d,ObsEncoder}.py | 将原 src/models.py 按职责拆分为 src/model/ 子包：modules.py（FiLMResBlock1D + Down/Upsample1d + sinusoidal_embedding）、unet1d.py（ConditionalUnet1D）、ObsEncoder.py（ObsEncoder + PreNormBlock）。原 src/models.py 保留为空占位（无符号）。L19–21 历史行的 `src/models.py` 文件名不回溯改 | 用户讨论 | 是（c2_plan.md L83、L194、L390 + c2_coding_plan.md §src/models.py 拆为三段 + .claude/CLAUDE.md §File Ownership / §Per-File Decision Checklist 同步 已更新）|
| 2026-05-14 | src/train.py 早停 state machine | 两阶段早停：Phase 1 loss_ema (α=0.95) 监控，20-epoch 窗口相对降幅 < 1% 触发 Phase 2；Phase 2 每 20 epoch quick eval (20 episodes)，SR 连续两次未上升停 (patience=2)；epoch 200 上限。决策 2 (>= 算上升)、决策 4 (best ckpt 用 SR) 在 Phase 2 内生效 | 用户讨论 + quick eval 统计性质分析 (10 ep SD=0.158, 20 ep SD=0.112) | 是（§5 已更新）|
| 2026-05-14 | src/train.py loss_ema | α = 0.95（等效窗口 ~20 epoch，与触发判据时间尺度对齐） | 用户讨论 | 是（§5 已更新）|
| 2026-05-14 | src/train.py plateau 判定 | 20-epoch 窗口相对降幅：`(loss_ema[t-20] - loss_ema[t]) / loss_ema[t-20] < 0.02`；t < 20 时判定不启动 | 用户讨论 | 是（§5 已更新）|
| 2026-05-14 | src/train.py Phase 1 checkpoint | 仅 last（每 epoch 覆盖），无 best；Phase 2 触发后首次 eval 建立 best_sr 基线 | 用户讨论 | 是（§5 已更新）|
| 2026-05-14 | plan/c2_plan.md §6 quick eval episodes | §6 "Quick eval：10 episodes" 与 §5 "quick eval（20 episodes）" 内部冲突，统一为 20 episodes（与 §5 锁定值一致） | reviewer flag → 用户裁定 | 是（§6 L181 已改 10 → 20）|
| 2026-05-14 | src/train.py quick_eval 权重归属 | quick_eval 与 best checkpoint 选取均用 EMA copy。pseudo code 顶部 `# === 实例化 ===` 段补 `ema_model = EMA(model, decay=0.9999)` 占位；Phase 2 调用处写 `quick_eval(ema_model, obs_encoder, ...)`。plan §5 "best checkpoint 按 EMA 权重的 quick eval SR 选定" 与 §6 "Inference 配置：EMA 权重" 不动 | reviewer flag（pseudo `quick_eval(model, ...)` 与 plan §5 EMA 表述不一致）→ 用户裁定 B | 否 |
| 2026-05-14 | src/train.py Phase 1→2 切换 + 训练结束兜底 | (a) Phase 1 触发 plateau 后设 `last_eval_epoch = epoch - 20`，使切换当 epoch 立即触发首次 quick eval。(b) 200 epoch 上限耗尽时无条件做一次 quick_eval；若 `final_sr >= best_sr` 覆盖 best checkpoint。动机：phase 1 plateau 未达成而 epoch 用尽时 `best_sr` 仍为初始 -1.0，否则不会保存任何 best checkpoint | 用户讨论（reviewer flag plan §5 未覆盖此两点）| 否 |
| 2026-05-14 | src/train.py bf16 autocast 包裹范围 | inner loop 用 `with torch.autocast(device_type="cuda", dtype=torch.bfloat16):` 包住 `obs_encoder(obs)` 与 `loss_fn(model, action, obs)` 两行（即 forward + loss compute，含 MSE reduction 内的相加）。`loss.backward()` 在 autocast 上下文外。`optimizer.step()` / EMA update / `loss.item()` 同样在 autocast 外。属于 pseudo code 高价值逻辑流，不外包给 executor | 用户讨论（DP 官方 train workspace 全程 fp32 无 autocast 调用，不可作 reference；按 PyTorch 官方 mixed precision 文档标准用法）| 否 |
| 2026-05-14 | src/train.py mixed precision master weights | Master weights fp32：`model` 与 `obs_encoder` 实例化后**不**做 `.to(torch.bfloat16)`，参数保持 fp32 storage。autocast 上下文内 forward 计算用 bf16 临时 cast，参数本身不变。`loss.backward()` 产 fp32 梯度，`optimizer.step()` 在 fp32 上做 AdamW update。EMA shadow 已是 fp32。补 row 30 的隐含项 | 用户讨论（PyTorch 官方 mixed precision 文档标准用法；row 30 锁了 autocast 范围但未限定 master weights dtype）| 否 |
| 2026-05-14 | src/train.py EMA decay | `0.9997` 静态（修订 c2_plan §5 表锁定的 `0.9999`）。row 28 中 `EMA(model, decay=0.9999)` 仅为占位语义，本行才是 decay 数值锁定 | 用户讨论 | 是（§5 表 `EMA decay 0.9999` 行待用户改为 `0.9997`）|
| 2026-05-14 | src/train.py EMA 覆盖范围 | EMA shadow 同时覆盖 `model.parameters()` 与 `obs_encoder.parameters()`。两者均为 trainable backbone，inference 需配对 swap。row 28 仅写 `ema_model` 占位，未限定范围，此行补全 | 用户讨论 | 否 |
| 2026-05-14 | src/train.py 训练结束兜底 eval 触发条件 | 任意退出方式后均无条件做一次 `quick_eval_with_ema`：(a) 早停 break（`stale_count >= 2`）后、(b) epoch 200 上限耗尽后。若 `final_sr >= best_sr` 则覆盖 best checkpoint。修订 row 29 (b) "200 epoch 上限耗尽时" 的限定语义为 "任意退出后" | 用户讨论 | 否 |
| 2026-05-14 | src/dp.py + src/train.py `alpha_bar` 存储方案 | dp.py 待决策项第 1 项选 (c)：`dp.py` 暴露 `build_alpha_bar_schedule(T) → Tensor (T,)` 工具函数。`train.py` 在 setup 阶段实例化 `alpha_bar = build_alpha_bar_schedule(T=100).to(device)`，通过 closure 绑定到 `loss_fn = lambda m, a, o: dp_loss(m, a, o, alpha_bar)`，作为 `dp_loss` / DDIM sampler 形参传入。不写入 model `register_buffer`，model 类对 schedule 无感 | 用户讨论 | 否 |
| 2026-05-14 | src/train.py logging 节奏 | `LOG_INTERVAL = 6` step：per-6-step 记录 loss / lr / grad_norm / optim_step_norm / ema_shadow_norm / action_histogram / τ-bucketed per-sample loss。非来自文献或微观实验，是用户基于 wandb 写入成本与可视化粒度自设计的取值 | 用户讨论 | 否 |
| 2026-05-16 | src/train.py + configs/*.yaml 隐式锁定参数补录 | (1) AdamW `lr = 1.0e-4`、`weight_decay = 1.0e-6`：与 Diffusion Policy 官方 repo `diffusion_policy/config/train_diffusion_unet_image_workspace.yaml` 中 `optimizer.lr` / `optimizer.weight_decay` 一致。(2) scheduler `warmup_steps = 500`：与 DP 官方同 yaml `training.lr_warmup_steps` 一致。(3) `clip_grad_norm = 1.0`：**非 DP 默认**，DP 官方 `workspace/train_diffusion_unet_image_workspace.py` 无梯度裁剪；本项为用户自加的安全机制，取 PyTorch 训练栈常见默认值 1.0。(4) `tau_buckets = 10`（τ-binned per-sample loss 日志粒度）：用户自设计，与 [src/cfm.py](../src/cfm.py) τ 采样 $\tau \sim \mathcal{U}[0, 1)$ 配对，分桶 $[k/10, (k+1)/10)$ 共 10 个等宽桶内 mean `per_sample_loss`，便于观察 loss 随 τ 单调结构 | (1)(2) DP 官方 yaml WebFetch 核实 + (3) 用户自加 PyTorch 工程默认 + (4) 用户自设 | 否 |
| 2026-05-16 | src/eval.py + src/train.py + plan/* + configs/*.yaml | 取消 `quick_eval` / `quick_eval_with_ema` 函数名 与 "Quick eval" / "Full eval" 概念名分裂。代码层统一为单一 `evaluate(model, obs_encoder, task_name, seed, n_episodes, ...)`，参数化区分调用上下文（n_episodes / seed / N 步数）。Plan 文字层保留 "训练中 evaluate" / "训练后 evaluate" 标签，不入代码层。配置字段 `early_stop.quick_eval_episodes` 改名 `early_stop.eval_episodes` | 用户讨论 | 是（c2_plan §5/§6, c2_coding_plan §src/train.py / §src/eval.py 已更新；.claude/CLAUDE.md L59 / L78 / L79 已同步；src/train.py 命名已同步（L27 / L111 / L122 / L365 / L401），但 evaluate 实际签名迁移见 row 39）|
| 2026-05-17 | src/eval.py 与 src/train.py 之间 evaluate 签名归属 | 选择方案 C：[src/eval.py](../src/eval.py) `evaluate` 保持现有低层签名 `(model, obs_encoder, env, init_states_for_episodes, infer_fn, normalizer, r3m_model, device, collect_failure_videos, max_steps)`；[src/train.py](../src/train.py) `main()` 在训练主循环开始之前一次性实例化 LIBERO 环境、初始状态集合、`infer_fn` 闭包、`Normalizer`、R3M 模型，保存在 `main()` 局部作用域；训练主循环 phase 2 与训练结束兜底位置的训练中 evaluate 调用复用同一环境实例。理由：每 20 epoch 触发一次评估，环境复用对挂钟时间收益明显；训练后 evaluate 与 ODE 步数消融实验由独立脚本驱动，与 train.py 不共享调用栈，环境复用语义自然分离；保持"代码层仅保留单一 evaluate 函数"的方向 | 用户讨论 + reviewer 三方案对比 | 否（c2_plan §6 与 c2_coding_plan §src/train.py / §src/eval.py 已涵盖"训练中 evaluate / 训练后 evaluate"两类调用上下文，无需额外修订；实施层待 executor 完成 main() setup 段、`evaluate_with_ema` 形参扩展、result-key 解析适配，并依赖 §三 src/eval.py 4 项 OPEN 决策推进）|
| 2026-05-17 | src/data.py construct_eval_obs 签名 | 形参定为 split-form 10 参：(image_agent_current, image_wrist_current, joints_current, grippers_current, image_agent_prev, image_wrist_prev, joints_prev, grippers_prev, r3m_model, device)。函数体内按 §3.3 math layout prev-first 顺序拼 obs_features。eval.py:153-157 call site 不动 | reviewer flag (spec-level error: pseudo §6 形参形态未 lock) → 用户裁定 B | 否 (c2_plan §3.3 math 层不变；c2_coding_plan §src/data.py 待决策项 6 与 §src/eval.py 待决策项 1 答案落 decisions.md 即可，无需改 plan/*) |
| 2026-05-17 | src/train.py evaluate_with_ema 调用策略 + src/eval.py 待决策项 4 | (1) 训练中 evaluate (phase 2 quick eval) `seed_for_eval = epoch`。(2) 训练后 evaluate (final fallback) `seed_for_eval = epoch + 1`，与最后一次 phase 2 quick eval 的 `seed_for_eval = epoch` 错开 1 格；两次 `np.random.RandomState(seed).choice(N_all, n_episodes, replace=False)` 抽样在不同 seed 下高概率不重合。(3) `n_episodes = 20`（沿用 row 23 / row 27 锁定值，train.py caller 两处硬编码）。(4) `N_all = 50`：LIBERO 官方协议给出，等于 `task_suite.get_task_init_states(task_id)` 返回数组长度；caller 不硬编码该值，需要时直接读 `len(init_states_all)`。本行同时完成 row 39 "实施层待 executor 完成 main() setup 段、`evaluate_with_ema` 形参扩展、result-key 解析适配" | 用户讨论 + LIBERO 官方协议（`task_suite.get_task_init_states` 长度）| 否（c2_plan §6 已涵盖训练中 / 训练后 evaluate 两类调用上下文；seed_for_eval 选取策略与 `N_all = 50` 属实施细节，不需回写 plan）|
| 2026-05-17 | src/eval.py evaluate `max_steps` 默认值 + Per-File Decision Checklist eval.py 第 2 项 | (1) [src/eval.py](../src/eval.py) `evaluate(...)` `max_steps` 形参末位 keyword + default=300（补 row 39 仅锁签名顺序未锁默认值的缺口）。(2) `eval.py` CLI `--max_steps` 默认 300，与函数 default 一致。(3) 与 .claude/CLAUDE.md §Per-File Decision Checklist eval.py 第 2 项 "Episode 最大步数上限（如 300 或 400）" 对齐，锁定 300 而非 400 | 用户裁定（executor spec 中 `--max_steps: int，默认 300`；与 LIBERO 官方 benchmark 常见 horizon 一致）| 否（Per-File checklist 第 2 项答案落 decisions.md 即可，无需改 plan/*）|
| 2026-05-17 | src/eval.py rollout 内 obs batch-dim 注入归属 + Per-File Decision Checklist eval.py 第 1 项延伸 | 选择方案 B：[src/eval.py](../src/eval.py) `rollout` 在 encoder 调用处对 `construct_eval_obs` 返回的 `(8210,)` 张量做 `.unsqueeze(0)` 升至 `(1, 8210)` 后再传入 `obs_encoder`。`construct_eval_obs` 仍按 row 40 锁定的 split-form 10 参签名返回 `(8210,)` 单样本张量（docstring 不变）；ObsEncoder 输入契约仍按 row 21 锁定的 $\mathbb{R}^{B \times 8210}$ 2D 形态（forward 不加 1D 兜底）。理由：与 train 侧 `__getitem__` 返回 `(8210,)` 由 DataLoader 升维的分工对称——rollout 承担 batch-dim 注入角色，类比 DataLoader 之于 train；与 row 21、row 40 zero impact，单行改动 | 用户裁定 B（reviewer 三方案过筛后 Option 3 与 row 21 冲突排除）| 否 |
| 2026-05-17 | src/dp.py `ddim_sample` timesteps 子采样策略 + src/train.py + src/eval.py caller | HuggingFace `DDIMScheduler` 默认 `timestep_spacing="leading"`, `steps_offset=0`：`step_ratio = T // T_infer; timesteps = (torch.arange(0, T_infer) * step_ratio).flip(0).tolist()`。代入 $T = 100$, $T_{\text{infer}} = 16$ → `timesteps = [90, 84, 78, 72, 66, 60, 54, 48, 42, 36, 30, 24, 18, 12, 6, 0]`。索引语义与 [src/dp.py:111-122](../src/dp.py#L111-L122) `t = int(timesteps[i]); ab_t = alpha_bar[t]; t_norm = (t + 1) / T` 对齐 $[0, T-1]$ 整数索引 + $+1$ 归一化。caller 落 [src/train.py:243-247](../src/train.py#L243-L247) 与 [src/eval.py:243-246](../src/eval.py#L243-L246) | HuggingFace [diffusers/scheduling_ddim.py](https://github.com/huggingface/diffusers/blob/main/src/diffusers/schedulers/scheduling_ddim.py) `set_timesteps` `__init__` 默认 + DP 官方 [diffusion_unet_lowdim_policy.py](https://github.com/real-stanford/diffusion_policy/blob/main/diffusion_policy/policy/diffusion_unet_lowdim_policy.py) 不手动构造 timesteps、直接 `scheduler.set_timesteps(num_inference_steps)` 后 `for t in scheduler.timesteps:` 复用 leading 默认 | 是（.claude/CLAUDE.md §Per-File Decision Checklist src/dp.py 待补第 4 项 "DDIM timesteps 子采样策略"；plan/c2_coding_plan.md §src/dp.py 待决策项同补；plan/* 与 .claude/CLAUDE.md immutable，待用户亲改）|
| 2026-05-17 | src/train.py 常量参数化 + configs/*.yaml 配置载体迁移 | [src/train.py](../src/train.py) 内 22 处硬编码常量替换为 `cfg.*` 字段读取，配置载体改用 [configs/cfm_default.yaml](../configs/cfm_default.yaml) 与 [configs/dp_default.yaml](../configs/dp_default.yaml)。覆盖字段：`optimizer.lr / optimizer.weight_decay`（row 37 (1)）、`scheduler.warmup_steps / scheduler.epoch_max`（row 23 / row 37 (2)）、`ema.decay`（row 32）、`train.grad_clip_norm / train.log_interval / train.tau_buckets`（row 36 / row 37 (3)(4)）、`loss_ema.alpha`（row 24）、`early_stop.{phase1_min_epoch, plateau_window, plateau_threshold, phase2_eval_interval, eval_episodes, patience}`（row 23 / row 25 / row 27 / row 41 (3)）、`dp.T`（row 35）。常量块 `EMA_DECAY / WARMUP_STEPS / LOG_INTERVAL / PLATEAU_THRESHOLD`（原 train.py L301-304）整段删除。原各 row 锁定的数值不变，本行仅迁移取值入口；yaml 与 train.py 同步唯一来源、消除 .claude/CLAUDE.md §src/train.py 行 "首轮训练后若需调整，改 cfg `early_stop.*` 字段" 此前的悬空承诺。`data.{chunk_horizon, obs_horizon, K_aug}` 三字段（cfm_default.yaml row 24-27）不入本次迁移：触及 [src/data.py](../src/data.py) `make_dataloader` 签名与 `LiberoGoalDataset.__init__` 转参，属高价值代码段，按 .claude/CLAUDE.md §File Ownership src/data.py 行 "高价值代码：sliding window padding ... dataset indexing 逻辑" 范围 raise 后用户裁定 deferred | 用户讨论（核实 train.py 22 处硬编码 vs yaml spec 漂移；用户裁定 yaml 权威；用户授权动 plan/c2_coding_plan.md immutable，但实际 c2_coding_plan.md 不含常量字面值，伪代码源为本文件 row 23/24/25/27/32/35/36/37/41，故本次只追加本行，不动 immutable 文件）| 否（.claude/CLAUDE.md §src/train.py 行原有 "首轮训练后若需调整，改 cfg `early_stop.*` 字段" 措辞由此承诺被代码兑现，无需修订；c2_plan.md §5 表 `EMA decay 0.9999` 行 row 32 已注的"待用户改"项不在本次范围）|
| 2026-05-17 | configs/*.yaml + src/train.py + src/eval.py 同义内容合并 | 修复 train.py 与 eval.py / scripts/precompute_r3m.py 间数值同义但分散的漂移点。yaml 新增三段：`infer.{N, T_infer, action_steps}`、`eval.max_steps`、`libero.{camera_height, camera_width}`。[src/train.py](../src/train.py) 改动：(a) load_cfg 删 `raw["infer"].setdefault("N", 4) / setdefault("T_infer", 16)`（yaml 显式给后双轨消除）；(b) `OffScreenRenderEnv(camera_heights/widths)` 读 `cfg.libero.camera_height / camera_width`（原硬编码 128）；(c) `evaluate_with_ema` 签名末位加 `max_steps, action_steps` 必传形参，内部转给 [src/eval.py](../src/eval.py) `evaluate`；phase 2 quick eval 与 final fallback eval 两处 caller 均显式传 `max_steps=cfg.eval.max_steps, action_steps=cfg.infer.action_steps`。[src/eval.py](../src/eval.py) 改动：(a) argparse `--N / --T_infer / --num_episodes / --max_steps` default 全改 `None`；main 内 step 1.5 新增三级 fallback resolution `CLI 显式 > ckpt cfg > (无)`：`N = args.N if args.N is not None else infer_cfg.N` 等 4 项；(b) `squared_cosine_schedule(T=100)` 改 `T=cfg_dict["dp"].T`；(c) `OffScreenRenderEnv(camera_heights/widths)` 改 `libero_cfg.camera_height / camera_width`；(d) `evaluate` 签名 `max_steps = 300` 改必传 + 加 `action_steps` 必传；(e) `rollout` 签名加 `action_steps` 形参（位置：`max_steps` 后），内部两处 `step % 8` 改 `step % action_steps`。`action_steps = 8` 与 `chunk_horizon = 16` 在 row 23 / row 41 内未显式声明耦合关系，本行明确两者解耦（用户裁定：8 为独立设定值，非派生）。`max_steps = 300` 沿用 row 42 锁定。`camera_*= 128` 数值不变，仅迁移取值入口。`infer.N = 4 / T_infer = 16` 数值沿用 row 41 (3) 与 [src/cfm.py](../src/cfm.py) / [src/dp.py](../src/dp.py) caller idiom，本行只将 train.py setdefault 升格为 yaml 显式字段。**不在本次范围**：(i) `scripts/precompute_r3m.py` `K = 8 / BRIGHTNESS = CONTRAST = 0.3 / TARGET_SIZE = 224 / FEATURE_DIM = 2048 / N_VIEWS = 2 / SEED = 42`（cache schema 强耦合，写盘即锁，参数化的运行时价值低；用户裁定 C 组 deferred）；(ii) `max_period = 10000 / num_res_blocks = 2 / GroupNorm(8) / nn.Linear(2048|7|2, dim) / nn.Embedding(2|4, dim) / s = 0.008` 等模型结构 fixture（物理量纲 / 论文锁定 / c2_plan 结构层不可调，用户裁定 D 组）| 用户讨论 + 用户裁定（A 组 2 处必改 + B 组 4 处推荐改 + C/D 组 deferred）| 否（c2_plan / c2_coding_plan / .claude/CLAUDE.md immutable；`.claude/CLAUDE.md §Per-File Decision Checklist src/infer.py` 第 1 项 "rollout 与 eval.py 的职责边界" 仍 OPEN（receding horizon stride 参数化是其副产物落地，非答案本身））|
| 2026-05-19 | src/train.py L576 + configs/{dp,cfm}_default.yaml + plan/c2_plan.md §5 L171 | row 23 决策 2 修订：phase 2 SR 上升判定 `sr >= best_sr` → `sr >= best_sr + cfg.early_stop.sr_uplift_margin`。margin = 0.05（两 yaml 同值）入 `early_stop.sr_uplift_margin` 字段，row 45 锁定的 cfg 字段集协同扩展。动机：原 `>=` 在 SR 触顶 ($sr = best\_sr = 1.0$) 后 `stale_count` 永远归零、`patience = 2` 触发不了，导致 `scheduler.epoch_max = 800` 成为唯一退出条件（本次 run `dp_turn_on_the_stove_seed42` 4h+ best_sr=1.0 仍未停 → 用户 raise）。新判定要求新 SR 高出 best 至少 5pp 才算上升，触顶 case `1.0 < 1.0 + 0.05` 不算上升，stale 累 → break。5pp margin 量值参 row 23 quick eval 噪声分析（20 ep $\text{SD} = 0.112$），新 SR 与 best_sr 差落在约 $0.5 \sigma$ 内不算显著上升。Caller 单点 [src/train.py:576](../src/train.py#L576)。row 23 原行 `>=` 字面值由本行修订，原行不回溯改；本行后 row 23 决策 2 字面以本行为准 | 用户讨论（2026-05-19 user raise SR 触顶后早停失效；user 授权同步动 immutable 文件）| 是（plan/c2_plan.md §5 L171 `>=` 算上升 → `sr >= best_sr + 0.05` 算上升 (margin 入 cfg)，本次用户授权动 immutable 文件已落；.claude/CLAUDE.md §Per-File Decision Checklist src/train.py 第 2 项"严格大于还是大于等于"待决策项原文不变（列项是枚举，本行只更新答案）；c2_coding_plan.md 不涉及 `>=` 字面值，不动）|
